import '../css/app.css';
import './bootstrap';

// Убираем предзагрузку видео - она не нужна
// import { videoManager, ALL_VIDEOS } from './services/videoManager.js';

// if (typeof window !== 'undefined') {
//   setTimeout(() => {
//     videoManager.preloadAll(ALL_VIDEOS).then(() => {
//       console.log('All background videos preloaded');
//     });
//   }, 1000);
// }
